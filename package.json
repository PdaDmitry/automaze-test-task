{
  "name": "automaze-test-task",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=client",
    "dev": "npm run dev --workspace=client"
  }
}
